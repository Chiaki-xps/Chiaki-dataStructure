# 双端队列（Deque）

+ 队列（Queue）结构，它是一种受限的线性结构，并且限制非常的严格。
+ 双端队列在单向队列的基础上解除了一部分限制：允许在队列的两端添加（入队）和删除（出队）元素
+ 滑动窗口问题`https://leetcode.cn/problems/hua-dong-chuang-kou-de-zui-da-zhi-lcof/`

# 优先级队列（Priority Queue）

+ 优先级队列（Priority Queue）是一种比普通队列更加高效的数据结构。
+ 它每次出队的元素都是具有最高优先级的，可以理解为元素按照关键字进行排序
+ 优先级队列可以用数组，链表等数组结构来实现，但是堆是最常用的实现方式（堆的效率高，虽然堆的本质也是数组实现的）

## 1. 优先级队列的实现

第一种：传入一个固定的优先级节点。

+ 创建优先级的节点，保存在堆结构中

+ 实际上就是堆放着优先级节点类的实例。

第二种：传入的对象，对象中必须有重写的`valueOf`,由这类对象决定返回比较的值



















